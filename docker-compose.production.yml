volumes:
  shoe_postgres_data: {}
  shoe_postgres_data_backups: {}
  shoe_django_media: {}
  shoe_django_staticfiles: {}
  
  shoe_redis_data: {}
  
networks:
  beyond_trend:

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/production/django/Dockerfile

    image: beyond_trend_shoe_django
    volumes:
      - shoe_django_media:/app/beyond_trend/media
    depends_on:
      - postgres
      - redis
    env_file:
      - ./.envs/.production/.django
      - ./.envs/.production/.postgres
    command: /start
    networks:
      - beyond_trend
    restart: always

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: beyond_trend_shoe_postgres
    volumes:
      - shoe_postgres_data:/var/lib/postgresql/data
      - shoe_postgres_data_backups:/backups
    env_file:
      - ./.envs/.production/.postgres
    networks:
      - beyond_trend
    restart: always

  redis:
    image: docker.io/redis:6
    
    volumes:
      - shoe_redis_data:/data
    
    networks:
      - beyond_trend
    restart: always

  celeryworker:
    <<: *django
    image: beyond_trend_shoe_celeryworker
    command: /start-celeryworker
    networks:
      - beyond_trend
    restart: always

  celerybeat:
    <<: *django
    image: beyond_trend_shoe_celerybeat
    command: /start-celerybeat
    networks:
      - beyond_trend
    restart: always

  flower:
    <<: *django
    image: beyond_trend_shoe_flower
    command: /start-flower
    networks:
      - beyond_trend
    restart: always

  nginx:
    build:
      context: .
      dockerfile: ./compose/production/nginx/Dockerfile
    image: beyond_trend_shoe_nginx
    depends_on:
      - django
    volumes:
      - shoe_django_media:/app/media
    ports:
      - 9119:80
    networks:
      - beyond_trend
    restart: always
